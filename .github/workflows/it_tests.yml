name: IT Tests

on:
  push:
    branches:
      - master
      - develop
  pull_request:
    branches:
      - master
      - develop
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'
        run: |
          cd ${{ env.HOME }}
          git clone https://ansotolab:${{ secrets.PAT }}@github.com/ansotolab/lca-gateway.git
          cd ${{ env.HOME }}/lca-gateway
          mvn spring-boot:run
          cd ${{ env.HOME }}
          git clone https://ansotolab:${{ secrets.PAT }}@github.com/ansotolab/lca-registry.git
          cd ${{ env.HOME }}/lca-registry
          mvn spring-boot:run
          cd ${{ env.GITHUB_WORKSPACE }}
          npm install -g newman
          newman run postman-customer.json